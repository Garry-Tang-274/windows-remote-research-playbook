# Deployment Status / 部署状态

The installer source is ready in this repository. Server-side deployment begins after the private destination repository and its deploy key are configured.

安装器源文件已在本仓库就绪。建立私有目标仓库并配置 Deploy Key 后，即可开始服务器端部署。
